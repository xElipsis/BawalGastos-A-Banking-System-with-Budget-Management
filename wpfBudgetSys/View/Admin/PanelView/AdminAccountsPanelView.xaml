<UserControl x:Class="wpfBudgetSys.View.Admin.PanelView.AdminAccountsPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource AppPageBrush}">
    <ScrollViewer Padding="24">
        <StackPanel>
            <Button Content="Refresh" Style="{StaticResource AppBackButtonStyle}" HorizontalAlignment="Right"
                    Command="{Binding RefreshCommand}" Margin="0,0,0,12"/>
            <DataGrid ItemsSource="{Binding Accounts}" AutoGenerateColumns="False" CanUserAddRows="False"
                      Style="{StaticResource BudgetCategoryDataGridStyle}" MinHeight="320">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Owner" Binding="{Binding OwnerName}" Width="*"/>
                    <DataGridTextColumn Header="Account #" Binding="{Binding AccountNumber}" Width="*"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding AccountType}" Width="80"/>
                    <DataGridTextColumn Header="Balance" Binding="{Binding Balance, StringFormat=N2}" Width="*"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80"/>
                    <DataGridTemplateColumn Header="Actions" Width="220">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Freeze" Style="{StaticResource AppBackButtonStyle}" Padding="8,4" Margin="0,0,4,0" FontSize="11"
                                            Command="{Binding DataContext.FreezeCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                    <Button Content="Unfreeze" Style="{StaticResource AppBackButtonStyle}" Padding="8,4" Margin="0,0,4,0" FontSize="11"
                                            Command="{Binding DataContext.UnfreezeCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                    <Button Content="Close" Style="{StaticResource AppBackButtonStyle}" Padding="8,4" FontSize="11"
                                            Command="{Binding DataContext.CloseAccountCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
