<UserControl x:Class="wpfBudgetSys.View.Admin.PanelView.AdminUsersPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource AppPageBrush}">
    <ScrollViewer Padding="24">
        <StackPanel>
            <Button Content="Refresh" Style="{StaticResource AppBackButtonStyle}" HorizontalAlignment="Right"
                    Command="{Binding RefreshCommand}" Margin="0,0,0,12"/>
            <DataGrid ItemsSource="{Binding Users}" AutoGenerateColumns="False" CanUserAddRows="False"
                      Style="{StaticResource BudgetCategoryDataGridStyle}" MinHeight="320">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding FullName}" Width="*"/>
                    <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80"/>
                    <DataGridTextColumn Header="Account" Binding="{Binding AccountNumber}" Width="*"/>
                    <DataGridTextColumn Header="Locked" Binding="{Binding IsLocked}" Width="60"/>
                    <DataGridTextColumn Header="Fails" Binding="{Binding FailedAttempts}" Width="50"/>
                    <DataGridTemplateColumn Header="Actions" Width="280">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Activate" Style="{StaticResource AppBackButtonStyle}" Padding="8,4" Margin="0,0,4,0" FontSize="11"
                                            Command="{Binding DataContext.ActivateCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                    <Button Content="Suspend" Style="{StaticResource AppBackButtonStyle}" Padding="8,4" Margin="0,0,4,0" FontSize="11"
                                            Command="{Binding DataContext.SuspendCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                    <Button Content="Unlock" Style="{StaticResource AppBackButtonStyle}" Padding="8,4" Margin="0,0,4,0" FontSize="11"
                                            Command="{Binding DataContext.UnlockCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                    <Button Content="Reset pwd" Style="{StaticResource AppBackButtonStyle}" Padding="8,4" FontSize="11"
                                            Command="{Binding DataContext.ResetPasswordCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
