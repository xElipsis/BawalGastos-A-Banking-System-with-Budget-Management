<UserControl x:Class="wpfBudgetSys.View.PanelView.AccountSettingsPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:uc="clr-namespace:wpfBudgetSys.View.UserControls"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900">

    <StackPanel>
        <Button Content="← Back to settings" Style="{StaticResource AppBackButtonStyle}" HorizontalAlignment="Left"
                Margin="0,0,0,16" Command="{Binding BackCommand}"/>

        <Border Style="{StaticResource AppFormCardStyle}">
            <StackPanel>
                <TextBlock Text="Profile" Style="{StaticResource AppSectionTitleStyle}" Margin="0,0,0,16"/>
                <uc:LabeledTextBox InputLabel="Full name" Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}"/>
                <uc:LabeledTextBox InputLabel="Email" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>
                <uc:LabeledTextBox InputLabel="Phone" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content="Save profile" Style="{StaticResource AppPrimaryButtonStyle}" Margin="0,8,0,24"
                        HorizontalAlignment="Left" Command="{Binding SaveProfileCommand}"/>

                <TextBlock Text="Login" Style="{StaticResource AppSectionTitleStyle}" Margin="0,8,0,16"/>
                <uc:LabeledTextBox InputLabel="Username" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content="Save username" Style="{StaticResource AppBackButtonStyle}" Margin="0,0,0,24"
                        HorizontalAlignment="Left" Command="{Binding SaveUsernameCommand}"/>

                <TextBlock Text="Change password" Style="{StaticResource AppSectionTitleStyle}" Margin="0,0,0,16"/>
                <uc:LabeledTextBox InputLabel="Current password" Text="{Binding CurrentPassword, UpdateSourceTrigger=PropertyChanged}"/>
                <uc:LabeledTextBox InputLabel="New password" Text="{Binding NewPassword, UpdateSourceTrigger=PropertyChanged}"/>
                <uc:LabeledTextBox InputLabel="Confirm new password" Text="{Binding ConfirmPassword, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content="Change password" Style="{StaticResource AppPrimaryButtonStyle}" Margin="0,8,0,0"
                        HorizontalAlignment="Left" Command="{Binding ChangePasswordCommand}"/>

                <TextBlock Text="{Binding StatusMessage}" Foreground="#DC2626" FontSize="13" Margin="0,16,0,0"
                           TextWrapping="Wrap"
                           Visibility="{Binding StatusMessage, Converter={StaticResource StringNotEmptyToVisibilityConverter}}"/>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
