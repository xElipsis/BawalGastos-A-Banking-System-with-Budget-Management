<UserControl x:Class="wpfBudgetSys.View.UserControls.LabeledTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="320">

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding InputLabel, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Style="{StaticResource LabeledTextBoxLabelStyle}"/>

        <TextBox x:Name="InputTextBox"
                 Grid.Row="1"
                 Style="{StaticResource LabeledTextBoxFieldStyle}"
                 IsReadOnly="{Binding IsReadOnly, RelativeSource={RelativeSource AncestorType=UserControl}}"
                 PreviewTextInput="TextBox_PreviewTextInput"
                 DataObject.Pasting="TextBox_Pasting"
                 Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl},
                        Mode=TwoWay,
                        UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="2"
                   Style="{StaticResource LabeledTextBoxErrorStyle}"
                   Text="{Binding ErrorMessage, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Visibility="{Binding ErrorMessage, RelativeSource={RelativeSource AncestorType=UserControl},
                            Converter={StaticResource StringNotEmptyToVisibilityConverter}}"/>
    </Grid>
</UserControl>
