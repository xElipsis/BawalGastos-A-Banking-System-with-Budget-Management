<UserControl x:Class="wpfBudgetSys.View.UserControls.LabeledTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:wpfBudgetSys.View.UserControls"
             mc:Ignorable="d"
             d:DesignWidth="300">
    
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Label Content="{Binding InputLabel, RelativeSource={RelativeSource AncestorType=UserControl}}"
               FontSize="14"/>
        
        <TextBox Grid.Row="1"
                 Height="40"
                 VerticalContentAlignment="Center"
                 PreviewTextInput="TextBox_PreviewTextInput"
                 DataObject.Pasting="TextBox_Pasting"
                 Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl},
                        Mode=TwoWay,
                        UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="2"
                   Foreground="Red"
                   FontSize="12" 
                   HorizontalAlignment="Right"
                   Text="{Binding ErrorMessage, RelativeSource={RelativeSource AncestorType=UserControl}}"/>

    </Grid>
</UserControl>
